import React from "react";
import { Styled } from "./hero.styled"

const Hero = () => {

    return (
        <Styled.Main id="home">
        <Styled.Heading>
            Hero
        </Styled.Heading>
        </Styled.Main>
    );
};

export default Hero;