export const size = {
  xs: "325px",
  ms: "550px",
  sm: "768px",
  md: "992px",
  lg: "1025px",
  xl: "1201px",
  x2l: "1400px",
  x3l: "1600px",
  x4l: "1800px",
  x5l: "2000px",
  x6l: "3000px",
  x7l: "4000px",
};

export const minimumDevice = {
  xs: `(min-width: ${size.xs})`,
  ms: `(min-width: ${size.ms})`,
  sm: `(min-width: ${size.sm})`,
  md: `(min-width: ${size.md})`,
  lg: `(min-width: ${size.lg})`,
  xl: `(min-width: ${size.xl})`,
  x2l: `(min-width: ${size.x2l})`,
  x3l: `(min-width: ${size.x3l})`,
  x4l: `(min-width: ${size.x4l})`,
  x5l: `(min-width: ${size.x5l})`,
  x6l: `(min-width: ${size.x6l})`,
  x7l: `(min-width: ${size.x7l})`,
};

export const maximumDevice = {
  xs: `(max-width: ${size.xs})`,
  ms: `(max-width: ${size.ms})`,
  sm: `(max-width: ${size.sm})`,
  md: `(max-width: ${size.md})`,
  lg: `(max-width: ${size.lg})`,
  xl: `(max-width: ${size.xl})`,
  x2l: `(max-width: ${size.x2l})`,
  x3l: `(max-width: ${size.x3l})`,
  x4l: `(max-width: ${size.x4l})`,
  x5l: `(max-width: ${size.x5l})`,
  x6l: `(max-width: ${size.x6l})`,
  x7l: `(max-width: ${size.x7l})`,
};

export const breakpoint = {
  breakpoints: {
    breakpointNames: {
      xs: `${size.xs}`,
      ms: `${size.ms}`,
      sm: `${size.sm}`,
      md: `${size.md}`,
      lg: `${size.lg}`,
      xl: `${size.xl}`,
      x2l: `${size.x2l}`,
      x3l: `${size.x3l}`,
      x4l: `${size.x4l}`,
      x5l: `${size.x5l}`,
      x6l: `${size.x6l}`,
      x7l: `${size.x7l}`,
    },
  },
};
